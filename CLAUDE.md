# MSReady Generator

## Project Structure

- `MSReady_Generator/` is the git repository root
- The working directory for Claude Code sessions is one level up (`260210-Desalter/`), so always use `MSReady_Generator/` as the git root for commits and pushes

## Key Files

| File | Description |
|---|---|
| `desalt_pipeline_v1.py` | Main pipeline module (CLI + importable library) |
| `data/L9910-Cosmetic Ingredient Compound Library-A.xlsx` | Input compound library |
| `data/Example_Output.xlsx` | Regenerated example output |
| `README.md` | Full documentation |
